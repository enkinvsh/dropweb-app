<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!--
      SECURITY: QUERY_ALL_PACKAGES is restricted by Google Play and should be
      justified in the submission form. It is used by the VPN plugin to build
      the per-app exclusion list ("apps that bypass the tunnel"). Keeping the
      permission so that UX doesn't break, but narrowing the tools:ignore so
      the lint check still runs. If Google Play rejects this, the exclusion
      list can fall back to the intents below via <queries>.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!--
      Minimum <queries> visibility so we can still list common app categories
      even if QUERY_ALL_PACKAGES has to be removed during Play review.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:name=".DropwebApplication"
        android:allowBackup="false"
        android:banner="@mipmap/ic_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="dropweb"
        android:memtagMode="off"
        tools:targetApi="s">
        <activity
            android:name="app.dropweb.MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
            <!--
              Deep links for subscription installation.
              NOTE: `android:autoVerify` cannot be set on custom schemes
              (it only works for http/https with a valid assetlinks.json).
              The `flclash://` scheme was removed as legacy cruft from the
              upstream fork; we keep `clash://` for ecosystem compatibility
              and `dropweb://` as our own.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="clash" android:host="install-config" />
                <data android:scheme="dropweb" android:host="install-config" />
            </intent-filter>
        </activity>

        <!-- Impeller disabled: two FlutterEngine instances (main UI + VPN service)
             both try to init Impeller Vulkan which hangs Surface creation on
             some devices (Pixel 10 at least), causing infinite splash.
             Custom shaders were removed, Skia handles everything we use. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!--
          SECURITY: TempActivity is a transparent activity used by our own
          home-screen widget and Quick Settings tile to relay start/stop
          intents into the VPN service. It MUST NOT be exported — otherwise
          any app on the device could toggle the VPN without user consent.
          Widgets and tile services in the SAME application (same UID) can
          still launch it via explicit ComponentName, which is what they do.
        -->
        <activity
            android:name=".TempActivity"
            android:exported="false"
            android:theme="@style/TransparentTheme">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.START" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.STOP" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="${applicationId}.action.CHANGE" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.DropwebTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher_monochrome"
            android:label="dropweb"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
            android:value="false" />
        </service>

        <!--
          SECURITY: BIND_APPWIDGET is a system-signature permission, which
          means only the Android AppWidget service can deliver broadcasts
          to this receiver. Without the permission, any app could spam
          APPWIDGET_UPDATE and force widget work.
        -->
        <receiver
            android:name=".widgets.DropwebWidgetProvider"
            android:exported="true"
            android:label="dropweb VPN"
            android:permission="android.permission.BIND_APPWIDGET">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/vpn_widget_info" />
        </receiver>

        <provider
            android:name=".FilesProvider"
            android:authorities="${applicationId}.files"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS"
            android:process=":background">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <service
            android:name=".services.DropwebVpnService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="vpn" />
        </service>

        <service
            android:name=".services.DropwebService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="service" />
        </service>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>